# 股票实时行情抓取与短线异动推荐 —— 设计文档

日期:2026-07-16
状态:待实现

## 1. 背景与目标

现有股票模块(`src/stock/`)的数据全部是日线(腾讯 `fqkline` 为主,东财 `klt=101` 兜底),`StockBar` 只有 `date` 字段、没有时间戳,不存在任何盘中数据通道。

本项目新增:

1. **实时行情抓取** —— 开市日 10:00–11:30、13:30–15:00,每 10 分钟抓取一次全 A 股快照
2. **数据保留** —— 实时数据只保留 5 个自然日,滚动淘汰
3. **短线异动推荐** —— 基于盘中突变筛出候选股,并标注「短线」或「长线」

### 非目标(明确排除)

- 不做分时明细/逐笔成交
- 不做长线选股(已有 `src/stock/recommend.rs`,不重复造)
- 不做策略验证/回测 —— 本次只产出信号与评估数据,验证是后续独立项目
- 不改动现有日线抓取链路

## 2. 现状约束(实现前必读)

| 约束 | 位置 | 影响 |
|---|---|---|
| 无任何实时数据通道 | 全 `src/stock/data/` 均为 `klt=101` / `,day,` | 抓取层是纯新建 |
| 无交易日历 | 不存在 `is_trading_day()`,cron 一周七天无条件触发 | 必须先解决「今天是否开市」 |
| 无短线/长线概念 | `evidence.rs:27` 写死 `HORIZON = 20`;`diagnose.rs:18` 写死 MA 20/60 | 标签逻辑是新建 |
| 择时逻辑未经验证 | `stock_advice.rs:3-11` 作者自述:阈值是「拍脑袋的魔数」,基金侧同款逻辑**上线后被证伪并删除** | 新阈值必须可配置,禁止写死 |
| web 层硬编码缓存路径 | `web/stock.rs:19,129-131` 用 `&'static Path` 绕过 config | 新代码不得重复此错 |
| 调度是阻塞线程 | `push/schedule.rs:1` 明确「不引入 tokio」 | 抓取须接入现有 60s tick 循环 |

## 3. 架构

新建 `src/stock/realtime/`,与 `src/stock/data/`(日线)并列,互不干扰。

```
src/stock/realtime/
├── mod.rs         公开接口
├── snapshot.rs    东财批量快照抓取
├── calendar.rs    交易日判断
├── store.rs       SQLite 读写 + 5 天清理
└── movers.rs      异动检测 + 短/长线标签
```

数据流:

```
push/schedule.rs (60s tick)
  └─> 到达抓取时点?
        └─> calendar::should_fetch()  ──非交易日──> 跳过
              └─> snapshot::fetch()   全市场快照
                    ├─> calendar 时间戳自证 ──非今天──> 标记节假日 + 丢弃
                    └─> store::insert_ticks()
                          └─> movers::detect()  异动候选
                                ├─> movers::classify()  短/长线标签
                                ├─> push (限流)
                                └─> Web API 随时查
              └─> store::prune()  删除 5 天前数据
```

## 4. 抓取层

### 4.1 数据源

东财批量快照:`https://push2.eastmoney.com/api/qt/clist/get`

`universe.rs:262` 已在用此接口抓港股列表,URL 拼法与 IPv4 绑定的既有实现可直接复用。调整 `fs=` 参数覆盖沪深主板 + 创业板 + 科创板。

取用字段:代码、名称、现价、涨跌幅、成交量、成交额、换手率、量比、行情时间戳。

**为什么是批量快照而非逐只轮询**:逐只 = 5400 × 20 = 10.8 万次请求/天,必被限流。批量 = 20 次/天。

复用现有 `kline.rs:59` 的 IPv4 强制绑定、8s 超时、2 次重试策略。

### 4.2 抓取时点

| 窗口 | 时点数 |
|---|---|
| 10:00 – 11:30 | 10(10:00, 10:10, … 11:30) |
| 13:30 – 15:00 | 10(13:30, 13:40, … 15:00) |
| 合计 | **20 / 交易日** |

接入 `push/schedule.rs` 现有 60 秒阻塞 tick 循环。

### 4.3 交易日判断(`calendar.rs`)

两道闸,零维护:

1. **周末本地跳** —— `chrono` 判定周六/周日,不发任何请求
2. **快照自证** —— 工作日发一次快照,比对返回的行情时间戳日期是否为今天。不是 → 判定为节假日,丢弃本次数据,将当日写入 `non_trading_days` 表,当天后续 19 个时点不再重试

**为什么不用硬编码节假日表**:每年年底必须手工更新,忘记则静默抓错。

**为什么不用 `universe::latest_trade_date()`**:它返回「最近已收盘日」,盘中取到的是昨天,无法回答「今天开不开市」。

## 5. 存储层

### 5.1 库文件

新建 `data/realtime.db`,与 `data/xlh.db`(账号/授权)物理隔离。

理由:
- 盘中每 10 分钟写 5400 行,不应与登录会话争同一 WAL 锁
- 纯派生数据,损坏可直接删除重建,不涉及用户资产

路径从 config 读取(**不得**照抄 `web/stock.rs` 的 `&'static Path` 硬编码)。

### 5.2 表结构

**`ticks`** —— 快照原始数据

| 列 | 说明 |
|---|---|
| `code` | 股票代码,联合主键 |
| `ts` | Unix 秒,联合主键 |
| `price` | 现价 |
| `change_pct` | 当日涨跌幅 |
| `volume` | 成交量 |
| `amount` | 成交额 |
| `turnover` | 换手率 |
| `vol_ratio` | 量比 |

`(code, ts)` 联合主键天然去重:同一时点重试不会写重。

**`non_trading_days`** —— 快照自证判定出的节假日,一行一个日期。

### 5.3 5 天保留

每个抓取周期结束后:

```sql
DELETE FROM ticks WHERE ts < ?;   -- 阈值 = 当前时刻 − 5 自然日
```

删除后执行 `PRAGMA incremental_vacuum`。**不可省略** —— 否则 SQLite 仅将页标记为空闲、文件不缩,长期运行磁盘只涨不落。

稳态数据量:5400 只 × 20 时点 × 5 天 ≈ 54 万行 / 30–50 MB。

**5 天口径 = 自然日**(非交易日)。理由:遇长假(如春节 7 天)时,自然日窗口仍能保留节前 2 天数据;交易日口径会让「5 天前」跨到两周前,异动对比的时间跨度失真。

## 6. 异动检测与短/长线标签

### 6.1 异动判定(`movers.rs`)

对每只股票取库中最近两个快照,计算:

- **10 分钟价格突变** = `(现价 − 上一时点价) / 上一时点价`
- **量能放大倍数** = `本时点成交量增量 ÷ 当日到此为止的平均每时点成交量增量`

**两者同时超阈值**才判定为异动。

理由:只看价不看量 → 捞进开盘半小时的正常波动;只看量不看价 → 捞到大宗横盘。

### 6.2 阈值配置化(硬性要求)

阈值**不得写死在代码中**,置于 `config.toml` 新增 `[realtime]` 段。

理由:`stock_advice.rs:3-11` 中作者自述其 10%/30% 系「拍脑袋的魔数」,且基金侧同款择时逻辑上线后被证伪删除。可配置化意味着可在积累数周真实数据后用 `evidence.rs` 前瞻检验框架调参,而非改代码重编译。

初始值:价格突变 ±2%,量能放大 3 倍。**此为待验证起点,不是结论。**

### 6.3 短/长线标签

异动仅负责筛候选;标签由现有模块判定:

- 拉取该股 `valuation.rs` 的 PE/PB 历史分位
- 拉取 `diagnose.rs` 的 MA20/MA60 趋势

判定规则:

| 标签 | 条件 |
|---|---|
| **长线** | 估值处于历史低分位 **且** 趋势非下跌 → 异动有基本面支撑 |
| **短线** | 其余全部 → 纯消息面/情绪驱动 |

候选每时点仅数十只,逐只拉取完全可承受,无需对 5400 只全算。

### 6.4 局限声明

该标签为启发式,与 `stock_advice.rs` 的买卖阈值同样**未经前瞻检验**。它是「分类线索」,不是「已验证策略」。真正验证需在第 5 节数据积累数周后另开一轮回测 —— 属后续项目,不在本次范围。

## 7. 交付层

三个出口共用同一份库数据。

### 7.1 Web API

新增 `GET /api/stock/realtime/movers`,返回当前异动榜:候选股 + 标签 + 触发时价量数据。

风格对齐现有 `src/web/stock.rs`:JSON API,`spawn_blocking` 包裹(数据层为 `reqwest::blocking`)。

### 7.2 盘中推送(限流)

接入现有飞书通道(`push/channels.rs`)。三层闸:

1. 同一只股票**当日只推一次** —— 已推记于内存 set,收盘清空
2. 只推**强信号**(≥ 阈值 1.5 倍),弱信号仅进库
3. 每时点**最多 5 只** —— 超出按突变幅度排序取前 5

无此三层则一天 20 时点 × 数十只 = 数百条,飞书必被刷屏至静音。

### 7.3 收盘汇总

15:00 后推送一条当日汇总,含:

- 当日触发过的全部异动股
- 各自标签
- 触发时突变幅度
- **收盘价相对触发点的涨跌**

最后一项是重点:它免费积累出信号质量评估数据 —— 异动信号发出后至收盘是涨是跌,数周后即可回答「这套阈值是否有效」。

### 7.4 免责声明

每个出口均带免责声明,与现有 `StockRecommendReport.disclaimer` 一致。

## 8. 配置

`config.toml` 新增:

```toml
[realtime]
db_path = "data/realtime.db"
retain_days = 5
price_jump_pct = 0.02      # 价格突变阈值,待验证
volume_surge_x = 3.0       # 量能放大阈值,待验证
strong_signal_x = 1.5      # 强信号 = 阈值的几倍
max_push_per_tick = 5
```

## 9. 测试

| 层 | 方式 |
|---|---|
| `calendar.rs` | 纯函数:给定日期/时间戳 → 是否抓取。周末、节假日、正常交易日、时间戳非今天 |
| `store.rs` | 内存 SQLite:写入去重、5 天边界清理(第 5 天保留、第 6 天删除) |
| `movers.rs` | 构造 tick 序列:仅价格超阈值不触发、仅量能超阈值不触发、两者超才触发 |
| `snapshot.rs` | 用固化的东财响应样本测解析,不打网络 |
| 推送限流 | 同股当日二次触发不推、弱信号不推、超 5 只截断 |

## 10. 风险

| 风险 | 缓解 |
|---|---|
| 阈值是猜的,信号可能全是噪音 | 阈值配置化;收盘汇总积累评估数据;文档明示未验证 |
| 东财接口变更/限流 | 复用现有重试+IPv4 绑定;抓取失败仅跳过该时点,不影响日线链路 |
| 库文件膨胀 | `incremental_vacuum` 强制回收 |
| 刷屏 | 三层限流闸 |
